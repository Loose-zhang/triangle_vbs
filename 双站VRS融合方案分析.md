# 双站VRS融合方案分析

## 方案定义

```
基站A (双频 L1+L5)          基站B (双频 L1+L5)
     ●  ←————5km以内————→  ●
          ↘              ↙
               ● C (虚拟点)
          AC距离 ≈ BC距离 ≤ 2.5km
```

**目标**：将A和B各自可见的卫星观测值，都"搬移"到虚拟点C，使C拥有A∪B的全部卫星集合。

---

## 第一层：空间误差搬移（几何部分）

对于基站A可见的卫星 $s$，将观测值从A搬到C的标准VRS公式：

$$\phi_C^s = \phi_A^s + (\rho_C^s - \rho_A^s) + (\delta_{trop,C}^s - \delta_{trop,A}^s) + (\delta_{iono,C}^s - \delta_{iono,A}^s)$$

在5km内各项误差空间相关性：

| 误差项 | 5km内空间相关性 | 可建模精度 |
|---|---|---|
| 对流层 | >99%相关 | mm级 |
| 电离层 | >95%相关 | 1~2mm（平静期） |
| 轨道误差 | 完全相关 | 忽略 |
| 多径 | 完全不相关 | 无法建模 |

**结论**：空间误差搬移对A和B各自可见的卫星，独立地均可实现，精度优于5mm。

---

## 第二层：模糊度问题

### 情形1：卫星 $s$ 仅A可见

$$\phi_{C,from\_A}^s = \phi_A^s + \Delta_{geom} + \Delta_{atm}$$

$$N_{C,from\_A}^s = N_A^s \quad \text{（继承自A，整数性保持）}$$

✅ 模糊度整数性完全保留。

### 情形2：卫星 $s$ 仅B可见

$$\phi_{C,from\_B}^s = \phi_B^s + \Delta_{geom} + \Delta_{atm}$$

$$N_{C,from\_B}^s = N_B^s$$

✅ 同上，整数性保留。

### 情形3：卫星 $s$ A和B均可见（重叠卫星）

两者之差：

$$\phi_{C,from\_A}^s - \phi_{C,from\_B}^s = (N_A^s - N_B^s) + (b_{r,A} - b_{r,B}) + \varepsilon$$

**问题**：来自A和B对同一颗卫星的虚拟观测值，存在未知的整数+小数偏差，两者不能直接混用。

---

## 第三层：接收机钟差——核心障碍

### 钟差的数学表达

$$\phi_A^s = \rho_A^s + c\delta t_A - c\delta t^s + \lambda N_A^s + \delta_{atm,A}^s + b_{r,A} - b^s + \varepsilon_A$$

$$\phi_B^s = \rho_B^s + c\delta t_B - c\delta t^s + \lambda N_B^s + \delta_{atm,B}^s + b_{r,B} - b^s + \varepsilon_B$$

搬移后，C点的虚拟观测值中混入了两个不同的接收机钟差。

### 跨站双差的问题

Rover与C做双差时：

| 情况 | 结果 |
|---|---|
| $s_A, s_B$ 均来自A | $\delta t_A$ 消除 ✅ |
| $s_A, s_B$ 均来自B | $\delta t_B$ 消除 ✅ |
| $s_A$ 来自A，$s_B$ 来自B | $\delta t_A - \delta t_B \neq 0$ ❌ |

两个独立接收机的钟差差值：

$$|\delta t_A - \delta t_B| \sim 10\text{ns} \sim 100\text{ns} \quad \Rightarrow \quad 3\text{m} \sim 30\text{m的等效距离误差}$$

这完全淹没了载波相位精度（mm级），RTK固定失败。

---

## 第四层：解决钟差问题的条件

### 方案A：外部精密时间同步

若A和B均配备原子钟，实现亚纳秒级时间同步：

$$|\delta t_A - \delta t_B| < 0.1\text{ns} \quad \Rightarrow \quad < 3\text{cm}$$

代价极高（原子钟约数十万元/台），工程实用性低。

### 方案B：利用公共卫星估计钟差之差（推荐）

对A和B均可见的公共卫星集合 $\mathcal{S}_{common}$，做站间差分：

$$\phi_A^s - \phi_B^s = (\rho_A^s - \rho_B^s) + c(\delta t_A - \delta t_B) + \lambda(N_A^s - N_B^s) + \Delta_{atm}^s + (b_{r,A} - b_{r,B})$$

利用多颗公共卫星，可以估计出 $c(\delta t_A - \delta t_B)$，然后将其作为系统性偏差从B的虚拟观测值中剥离。

**实施条件**：
- A和B之间至少有 **4颗公共可见卫星**
- A-B基线需要先解算（固定整周模糊度）
- 钟差差需要**逐历元更新**（接收机钟漂可达 ~1ns/s）

---

## 第五层：硬件频率偏差的处理

即使解决了钟差问题，仍有硬件频率偏差需要处理：

$$b_{r,A,L1} \neq b_{r,B,L1}, \quad b_{r,A,L5} \neq b_{r,B,L5}$$

**解决方法**：在估计 $c(\delta t_A - \delta t_B)$ 时，将 $(b_{r,A} - b_{r,B})$ 一并估计为整体偏差，作为额外参数统一改正。

---

## 综合可行性评估

| 子问题 | 可行性 | 条件 |
|---|---|---|
| 空间误差搬移（A、B各自卫星→C） | ✅ 完全可行 | 标准VRS技术 |
| 模糊度整数性保持（单站来源） | ✅ 完全可行 | 不混源双差 |
| 接收机钟差差估计 | ✅ 可行 | ≥4颗公共卫星 + 基线解算 |
| 跨站双差（混源）载波固定 | ⚠️ 有条件可行 | 钟差差改正后可尝试 |
| Rover使用C进行RTK固定 | ⚠️ 有条件可行 | 需精细处理偏差，且公共卫星≥4 |
| C扩展卫星数（A∪B） | ✅ 理论可行 | 上述条件全满足后 |

---

## 最终结论与实施建议

方案在理论上可行，但有严格前提：

### 必须满足的条件

1. **A-B基线必须先完成精密解算**（固定整周模糊度，得到精确基线向量）
2. **每个历元实时估计 $c(\delta t_A - \delta t_B)$**（利用公共卫星，≥4颗）
3. **Rover使用C做双差时，必须追踪每颗卫星的"来源"**（A or B）
4. **同源卫星间双差**正常消除钟差
5. **跨源卫星间双差**需加入钟差差改正项

### 工程降级策略

- 公共卫星少于4颗时，该历元不输出跨源卫星
- 将钟差差估计精度作为质量指标，超阈值时降级处理
- 优先选C在AB连线中点，使两站误差改正量对称，残差最小

### 本质认识

该方案本质上是一个**微型双基准站网络（Mini-CORS）**的雏形，与商业网络RTK（如VRS、FKP）的原理高度相似，区别仅在于只有两个站而不是三角网。**两站网络的几何强度弱于三站，但在5km基线内是工程可行的。**

---

## 关联背景

### 本方案的数据来源假设

| 数据 | 来源 |
|---|---|
| 基站A/B实测观测值 | MSM4（L1+L5） |
| 精密轨道/钟差改正 | SSR（如SSRC00CNE0、SSRC00WHU0） |
| 广播星历 | BRDC（如BCEP00BKG0） |
| 多频相位OSB | OSBC00WHU1（products.igs-ip.net） |

### 为何不能直接生成L2

L2模糊度建立需要L2实测载波相位，而基站仅有L1+L5输出，任何SSR或模型都无法替代物理观测。该双站融合方案仅扩展了可见卫星数量，并不解决频率缺失问题。若需L2，唯一路径是更换支持L2C的接收机（如Septentrio mosaic-X5、u-blox F9P等）。
